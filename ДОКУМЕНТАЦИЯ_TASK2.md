# Документация расчётных модулей — Задача №2

> **Задача №2.** Реальная вертикальная пластина с **толщиной δ**. С тыльной
> стороны равномерно подводится q_w = const от силиконовых матов; со всех
> остальных граней тепло сбрасывается свободной конвекцией и излучением.
> Найти распределение температуры T(z) по высоте и понять, какая доля
> подведённого тепла уходит через каждую грань.

Документ описывает три новых модуля (`correlations_horizontal.py`,
`task2_solver.py`, `plotting_task2.py`) и страницу
`pages/2_Задача_2_Реальная_пластина.py`. Опирается на уже существующие
модули Задачи №1 (`correlations_uhf.py`, `correlations_meta.py`,
`properties.py`).

---

## 1. Постановка задачи

### 1.1. Геометрия — 6 граней

Пластина b × L × δ (по умолчанию **100 × 2000 × 12 мм**).

| Грань | Размер | Площадь | Граничное условие |
|---|---|---|---|
| Тыльная | b × L | b·L | q_w = const (подвод от матов) |
| Лицевая | b × L | b·L | конвекция + радиация |
| Боковой торец (× 2) | δ × L | δ·L каждый | конвекция + радиация |
| Верхний торец | b × δ | b·δ | конвекция + радиация |
| Нижний торец («днище») | b × δ | b·δ | **только радиация** |

При L = 2 м, b = 0,1 м, δ = 0,012 м:
- A_фасад = 0,2 м² (большая)
- A_боковые = 0,048 м² (≈ 24% от A_фасада — нельзя пренебречь!)
- A_торцов вверх/вниз = 0,0012 м² каждая (мизерные, но учитываются)

### 1.2. Допущения и упрощения

1. **Изотермия поперёк толщины.** Bi = h·δ/λ_метал ≪ 0,1 для всех
   реалистичных параметров (для нержавейки λ = 16 и h = 10: Bi ≈ 0,008).
   → задача редуцируется к **1D вдоль высоты z**: T = T(z).
2. **Продольная теплопроводность учитывается** через λ_метал. Особенно
   важно у краёв (z=0, z=L), где ГУ отличаются.
3. **Лицевая грань и боковые торцы** — обе вертикальные поверхности,
   совпадают по высоте z и температуре T(z). → используется одна и та же
   локальная UHF-корреляция (одна из 8 из Задачи №1).
4. **Верхний торец** (горизонтальная нагретая грань лицом вверх) —
   корреляция МакАдамса с характеристическим размером L_c = A/P.
5. **Днище** (горизонтальная грань лицом вниз) — по ТЗ только излучение.
   В реальности конвекция там слаба (тёплый воздух не стекает), пренебречь
   физически оправдано.
6. **Излучение** на всех гранях с одной степенью черноты ε (можно сделать
   per-face в будущих версиях).

---

## 2. Математическая модель

### 2.1. Уравнение фина по высоте

В стационаре каждый элемент длины dz получает:
- от матов: q_w · b · dz
- от соседей по z: λ_м · A_кр · ∂²T/∂z²
- сбрасывает с вертикальных граней: [α_v(T)·ΔT + ε·σ(T⁴−T_∞⁴)] · (b + 2δ) · dz

Баланс:

$$
\lambda_\text{м}\,A_\text{кр}\,\dfrac{d^2T}{dz^2}
\;+\; q_w\,b
\;-\; \big[\alpha_v(T,z)\,\Delta T
       + \varepsilon\sigma(T^4-T_\infty^4)\big]\,(b+2\delta)
\;=\; 0
$$

где A_кр = b·δ — поперечное сечение металла, ΔT = T(z) − T_∞.

### 2.2. Граничные условия

**z = 0 (днище):** тепло через нижний торец уходит только излучением.
Кондуктив на нижней грани равен этой потере:

$$
-\lambda_\text{м}\,A_\text{кр}\,\dfrac{dT}{dz}\bigg|_{z=0}
= \varepsilon\,\sigma\,(T(0)^4-T_\infty^4)\,A_\text{дн}
$$

**z = L (верхний торец):** конвекция (МакАдамс) + радиация.

$$
+\lambda_\text{м}\,A_\text{кр}\,\dfrac{dT}{dz}\bigg|_{z=L}
= \big[\alpha_\text{верх}\Delta T + \varepsilon\sigma(T(L)^4-T_\infty^4)\big]\,A_\text{верх}
$$

### 2.3. Конвекция верхнего торца — Леонтьев 1979

Источник: Леонтьев А.И. (ред.) «Теория тепломассообмена», 1979,
**стр. 309, формула (7.30)** и стр. 312 (универсальная корреляция для
турбулентной зоны):

| Режим | Nu | Диапазон Ra_Lc | Источник |
|---|---|---|---|
| Ламинар | Nu = 0,54·Ra^(1/4) | 10⁵ ≤ Ra ≤ 2·10⁷ | стр. 309 (7.30) |
| Турбулент | Nu = 0,135·Ra^(1/3) | 2·10⁷ ≤ Ra ≤ 10¹³ | стр. 312, универс. |

Характеристический размер: $L_c = A/P$ (обобщение «стороны квадрата»
из источника на произвольную плоскую форму — Lloyd-Moran 1974, используется
во всех современных монографиях). Для прямоугольника b × δ:
$L_c = (b·δ)/(2(b+δ)) \approx 5{,}4$ мм при b=100, δ=12 мм.

Физические свойства воздуха — при плёночной температуре T_f = 0,5·(T_s + T_∞)
(явно указано в источнике после формулы 7.30).

При типовых параметрах задачи (T_s ≈ 195 °C, T_∞ = 20 °C, L_c ≈ 5,4 мм)
Ra ≈ 800 — ниже опубликованного диапазона (10⁵). Корреляция всё равно
применяется (ламинарная экстраполяция), но в `NuResult.regime`
проставляется флаг `'out'`. Влияние на итоговый баланс невелико:
верхний торец даёт <1% потерь.

### 2.4. Линеаризация радиации

Чтобы превратить нелинейное T⁴ в коэффициент при ΔT (нужно для
тридиагональной системы), используем тождество:

$$
T_s^4 - T_\infty^4 = (T_s + T_\infty)\,(T_s^2 + T_\infty^2)\,(T_s - T_\infty)
$$

→ $h_\text{рад}(T) = \varepsilon\,\sigma\,(T_s+T_\infty)(T_s^2+T_\infty^2)$.

Этот коэффициент пересчитывается на каждой Picard-итерации при
обновлении T(z).

---

## 3. Архитектура модулей

### 3.1. `correlations_horizontal.py`

```python
def nu_mcadams_horizontal_up(Ra) -> HorizNuResult
def alpha_horizontal_up(t_s_C, t_fluid_C, P_Pa,
                        area_m2, perimeter_m) -> (alpha, HorizNuResult)
```

Чистые функции, без зависимости от солвера.

### 3.2. `task2_solver.py`

Главный API:

```python
MATERIALS = {
    'Д16-Т (алюминий)':       {'lambda': 130.0, ...},  # дефолт
    'АМг3 (алюминий)':         {'lambda': 160.0, ...},
    'AISI 304 (нерж. сталь)':  {'lambda':  16.0, ...},
    'Медь М1':                 {'lambda': 400.0, ...},
}

@dataclass
class NodeResult:
    z_m, T_s_C, alpha_front,
    q_conv_per_m2, q_rad_per_m2,
    Ra_x, Gr_x_star, GrPr_star, regime

@dataclass
class Task2Result:
    # входные
    key, meta, material_name, lambda_metal,
    b_mm, L_mm, delta_mm, N_total_W, q_w,
    t_fluid_C, eps_surface
    # профиль
    nodes: list[NodeResult]
    # интегральные потери (Вт)
    Q_input_W
    Q_front_conv_W, Q_front_rad_W
    Q_sides_conv_W, Q_sides_rad_W
    Q_top_conv_W, Q_top_rad_W
    Q_bottom_rad_W
    # сходимость
    iterations, max_delta_K, converged
    # производные свойства (через @property)
    Q_front_W, Q_sides_W, Q_top_W, Q_bottom_W
    Q_output_W, residual_W, residual_pct
    T_max_C, T_min_C, T_avg_C

def solve_task2(*,
    key, N_total_W, b_mm, L_mm, delta_mm,
    lambda_metal, material_name='',
    t_fluid_C=20.0, P_Pa=101325.0, eps_surface=0.95,
    N_nodes=121, max_iter=40, tol_K=0.01, g=9.80665,
    relax=0.7, x_min_mm=10.0,
) -> Task2Result
```

#### Алгоритм Picard-итерации

```
T_0(z) ← начальное приближение (равновесная оценка)
for k = 1..max_iter:
    1. Для каждого z_i: вычислить α_v_i и h_rad_i при текущем T_k(z_i)
    2. Построить тридиаг. систему:
       (−κ)·T_{i−1} + (2κ + h_total_i·(b+2δ))·T_i + (−κ)·T_{i+1}
            = q_w·b + h_total_i·(b+2δ)·T_∞
       где κ = λ·A_кр/dz², h_total = α_v + h_rad
    3. ГУ через ghost-узлы (половинные элементы у границ).
    4. Решить методом прогонки (Thomas algorithm).
    5. T_{k+1} = (1−relax)·T_k + relax·T_new
    6. Если max|T_{k+1} − T_k| < tol_K: break
```

Тридиаг. решатель `_thomas(a, b, c, d)` — отдельная чистая функция.

#### Постпроцесс

После сходимости — для каждого узла вычисляются точные q_conv и q_rad
(нелинейная радиация, без линеаризации). Интегрирование трапецией
по высоте даёт потери Q_front, Q_sides; для торцов — q × A.

**Контроль качества:** `residual_pct = (Q_in − Q_out)/Q_in · 100%`.
При сходимости residual_pct ≈ 0 (типично < 0,01%).

### 3.3. `plotting_task2.py`

```python
def plot_temperature_profile(result) -> plotly.graph_objects.Figure
```

- Кривая T(z), z по вертикальной оси, T по горизонтальной.
- Фон — цветные горизонтальные полосы по режиму конвекции лицевой грани
  (lam/trans/turb/out) на данной z. Полосы наследуют палитру Задачи 1
  (просто более прозрачные).
- Линия T_∞ (пунктир), маркеры T_max и T_min с подписями.
- Легенда из 4 chip-маркеров (зоны режима).

```python
def plot_heat_balance(result) -> plotly.graph_objects.Figure
```

- Один горизонтальный stacked-bar.
- 7 сегментов: front conv, front rad, sides conv, sides rad,
  top conv, top rad, bottom rad.
- Цвета — публикационная палитра (3 пары «конвекция/радиация» +
  отдельный для днища).
- Внутри сегмента — текст «N,N%» (если ≥3%).
- Под графиком — легенда с детализацией (название · Вт · %).
- Подпись «Σ = q_w·b·L = N Вт (вход)» — для контроля.

### 3.4. `pages/2_Задача_2_Реальная_пластина.py`

Структура страницы:

1. **Шапка** — eyebrow, серифный заголовок, лиде.
2. **Ввод**: 4 поля (b, L, δ, N) в одной строке + 2 dropdown (материал,
   методика) в следующей.
3. **Доп. параметры** (expander): t_∞, P, ε, N_nodes, λ_металла,
   x_min_mm.
4. **KPI-полоса** под заголовком: q_w, T_min, T_avg, T_max, Q_вход,
   невязка, число итераций. Все числа моноширинно.
5. **Шаг 1 · Распределение температуры**: график T(z) во всю ширину.
6. **Шаг 2 · Баланс тепла**: stacked-bar + таблица (грань × механизм ×
   Q × % × площадь).
7. **Раздел 3 · Используемые формулы**: expander с LaTeX (фин-уравнение,
   ГУ, выбранная UHF-формула, МакАдамс, радиация).

Кэширование `@st.cache_data` поверх `_run(...)` — повторное открытие
страницы при том же наборе параметров не пересчитывает.

---

## 3a. Алгоритм пошагово

### Шаг 0 · Исходные данные

Из заданных b, L, δ, N (мощности матов) вычисляется удельный
тепловой поток:

    q_w = N / (b·L)         [Вт/м²]

Площади и периметры:

    A_кр = b·δ              — поперечное сечение металла
    P_v  = b + 2δ           — суммарная конвективная «ширина» вертикальных граней
                              (лицевая + 2 боковых торца)
    A_дн = b·δ              — площадь нижнего торца
    A_верх = b·δ            — площадь верхнего торца
    L_c   = A_верх/P_верх   — характерный размер для верхнего торца

### Шаг 1 · Дискретизация

Высота L разбивается на N узлов: $z_i = i \cdot dz$, $dz = L/(N-1)$, $i=0..N-1$.
В каждом узле ищется одна температура $T_i = T(z_i)$.

### Шаг 2 · Начальное приближение

Грубая оценка $\Delta T \approx q_w b /[h \cdot P_v]$ при $h \approx 8$ Вт/(м²·К)
даёт стартовое $T_0(z) = T_\infty + \Delta T$, одинаковое во всех узлах.
Стартовая точка не критична — Picard сходится из почти любого
осмысленного приближения.

### Шаг 3 · Picard-итерация (`k → k+1`)

**3.1. Локальные коэффициенты при текущем $T_k(z)$:**

  (а) Вертикальные грани. Для каждого узла $z_i$:
      • Свойства воздуха при опорной T (плёночной либо t_∞ — зависит от
        методики; для Керимова 1992 — при t_∞).
      • Gr_x, Gr_x*, Ra по выбранной UHF-методике из Задачи 1.
      • Nu_x → $\alpha_v(z_i, T_i) = \text{Nu}_x · \lambda / x_\text{эфф}$,
        где $x_\text{эфф} = \max(z_i, x_\text{min})$ — защита от
        сингулярности в самом низу.
      • Эпсилон-поправка $\varepsilon_t$ для Керимова 1992; передаётся
        в Nu-функцию, остальные методики её игнорируют.

  (б) Линеаризованная радиация:
      $h_\text{рад}(T_i) = \varepsilon\sigma(T_i + T_\infty)(T_i^2 + T_\infty^2)$
      — тождественна $\varepsilon\sigma(T_i^4 - T_\infty^4)/(T_i - T_\infty)$.

  (в) Эффективный коэф. потерь на вертикальной грани:
      $h_\text{tot}(z_i) = \alpha_v(z_i, T_i) + h_\text{рад}(T_i)$.

  (г) Верхний торец: $\alpha_\text{верх}(T_{N-1})$ по Леонтьеву 1979 (7.30),
      далее $h_\text{верх} = \alpha_\text{верх} + h_\text{рад}(T_{N-1})$.

  (д) Днище: только радиация, $h_\text{дн} = h_\text{рад}(T_0)$.

**3.2. Построение тридиагональной системы.**
Для интерьерного узла $0 < i < N-1$:

    -κ · T_{i-1}  +  (2κ + h_tot_i · P_v) · T_i  +  -κ · T_{i+1}
        = q_w · b  +  h_tot_i · P_v · T_∞

где $\kappa = \lambda_\text{м} A_\text{кр}/dz^2$.

Для **граничных** узлов используется техника «полу-элемента»: добавляется
тепло, уходящее через торец. У узла $i=0$ диагональ увеличена на
$2 h_\text{дн} A_\text{дн}/dz$, у узла $i=N-1$ — на $2 h_\text{верх} A_\text{верх}/dz$.
Внедиагональные элементы: $a_0=0$, $c_0=-2\kappa$, $a_{N-1}=-2\kappa$,
$c_{N-1}=0$ (зеркальный ghost-узел).

**3.3. Решение тридиаг. системы** методом прогонки (Thomas algorithm) —
функция `_thomas(a, b, c, rhs)`. Результат — «свежий» $T_\text{new}(z)$.

**3.4. Релаксация:**
$T_{k+1} = (1-\omega) T_k + \omega T_\text{new}$, $\omega = 0{,}7$.

### Шаг 4 · Проверка сходимости

Если $\max_i |T_{k+1,i} - T_{k,i}| < \text{tol}_K$ (по умолчанию 0,01 К)
— итерация остановлена. Иначе — переход к Шагу 3 с новым $T_k$.
На дефолтных параметрах сходится за 7-16 итераций.

### Шаг 5 · Постпроцесс — точные потоки

По сошедшемуся $T(z)$ для каждого узла пересчитываются **точные** (не
линеаризованные) тепловые потоки:

    q_конв(z_i) = α_v(z_i, T_i) · (T_i - T_∞)
    q_рад(z_i)  = ε·σ·(T_i⁴ - T_∞⁴)

Интегрирование трапецией по z даёт суммарные потери:

    Q_лиц_конв = ∫ q_конв · b dz             (лицевая грань)
    Q_лиц_рад  = ∫ q_рад  · b dz
    Q_бок_конв = ∫ q_конв · 2δ dz            (2 боковых торца)
    Q_бок_рад  = ∫ q_рад  · 2δ dz
    Q_верх_конв = α_верх · ΔT · A_верх       (при T(L))
    Q_верх_рад  = ε·σ·(T(L)⁴ - T_∞⁴) · A_верх
    Q_дн_рад   = ε·σ·(T(0)⁴ - T_∞⁴) · A_дн

### Шаг 6 · Контроль баланса

    Q_вх    = q_w · b · L
    Q_сброс = Q_лиц + Q_бок + Q_верх + Q_дн
    resid   = (Q_вх - Q_сброс) / Q_вх

При сошедшейся итерации resid < 0,01%. Это **независимая** проверка
правильности решения (солвер сам её не использует).

---

## 4. Поток данных

```
[User input]
   b, L, δ, N, material, methodology, t_∞, P, ε, N_nodes, λ_металла
        ↓
[solve_task2 ─ Picard iteration]
   q_w = N/(b·L)
   T_0 = T_∞ + ΔT_guess (по «грубому» балансу)
   loop:
      for each z: α_v, h_rad ← compute_nu(key, …)
      α_top, h_top_rad ← alpha_horizontal_up(...)
      build tridiag system (Thomas)
      solve → T_new
      relax: T = (1−ω)·T_old + ω·T_new
      converged when max|ΔT| < tol_K
   ↓
[Task2Result]
   nodes[i]: z, T_s, α, q_conv, q_rad, regime
   Q_front_conv, Q_front_rad, Q_sides_*, Q_top_*, Q_bottom_rad
   residual_pct (контроль)
        ↓
[plot_temperature_profile] + [plot_heat_balance] + [df]
   три артефакта в UI
```

---

## 5. Smoke-результаты на дефолтах

`b=100, L=2000, δ=12 мм; Д16-Т (λ=130); N=1000 Вт; t_∞=20 °C; ε=0,95;
методика — Керимов 1992; N_nodes=121`:

| Метрика | Значение |
|---|---|
| q_w | 5000 Вт/м² |
| T_avg | 193,9 °C |
| T_min | 188,1 °C (вблизи z=0) |
| T_max | 199,5 °C |
| Q_вход | 1000,0 Вт |
| Итераций | 7 |
| Невязка | < 0,01 % |

Распределение потерь:

| Путь | Q, Вт | % |
|---|---|---|
| Лицевая (конвекция) | 367,6 | 36,8 |
| Лицевая (радиация) | 432,0 | 43,2 |
| 2 боковых торца (конвекция) | 88,2 | 8,8 |
| 2 боковых торца (радиация) | 103,7 | 10,4 |
| Верхний торец (конвекция) | 3,5 | 0,4 |
| Верхний торец (радиация) | 2,5 | 0,2 |
| Днище (радиация) | 2,5 | 0,2 |
| **Σ** | **1000,0** | **100,0** |

Чувствительность к материалу:

| Материал | λ, Вт/(м·К) | ΔT по высоте, К |
|---|---|---|
| AISI 304 (нерж. сталь) | 16 | 36,6 (резкий профиль) |
| Д16-Т (алюминий) | 130 | 11,4 |
| Медь М1 | 400 | 4,9 (почти изотермична) |

Все три прогона сходятся за 7-16 Picard-итераций; невязка остаётся
< 0,001 % — баланс закрывается строго.

---

## 6. Что можно расширить

1. **Per-face степень черноты** — сейчас одна ε для всех граней.
   Реальная пластина может иметь матовую лицевую и полированную тыльную
   стороны.
2. **Per-face температурная зависимость свойств** — сейчас свойства
   воздуха берутся одинаково для лицевой и боковых граней (по T(z)).
3. **Учёт q_w(T_s) через ТКС** — если матов нет, а нагрев — Джоулев
   (как в Задаче 1), нужно q_w(T_s).
4. **Сравнение нескольких методик одновременно** — сейчас одна методика
   на расчёт. Можно сделать оверлей нескольких T(z).
5. **Тыльная грань с потерями** — сейчас идеализация «всё q_w идёт в
   пластину». Реально маты имеют изоляцию с конечным R_тыл; можно
   добавить отдельный путь потерь.
