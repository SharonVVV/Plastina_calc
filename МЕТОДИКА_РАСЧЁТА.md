# Методика расчёта теплового баланса вертикальной нагреваемой пластины

> ⚙ **LEGACY-документ.** Описывает исходный UI (`app.py` + 8 вкладок) и
> модули `solver.py`, `correlations.py`, `turbulence_solver.py`,
> `qconst_page.py`, `back_losses.py`, `comparison_page.py`,
> `verification_*.py`. Это рабочая реализация стенда Керимова для
> Джоулева нагрева, **сохраняется без изменений** и доступна в Streamlit
> на главной странице приложения.
>
> Для **Задачи №1** (новая страница `pages/1_Задача_1_Турбулентность.py`
> — сравнение восьми локальных UHF-корреляций) — см. **`ДОКУМЕНТАЦИЯ_TASK1.md`**.
> Та задача использует независимые модули `correlations_uhf.py`,
> `correlations_meta.py`, `task1_solver.py`, `plotting_task1.py`.

> Стенд Керимова, НИУ «МЭИ».  
> Программная реализация: `Plastina_calc`

---

## 1. Постановка задачи

Вертикальная металлическая пластина высотой $L$ и шириной $b$ обогревается переменным электрическим током $I$.  
Тепло отводится **свободной конвекцией воздуха** и **излучением** с поверхности.

**Задача:** для каждого сечения по высоте $x$ найти температуру стенки $t_{c,x}$, при которой выполняется **тепловой баланс:**

$$
q_{\text{эл}}(t_c) = q_{\text{конв}}(t_c) + q_{\text{рад}}(t_c)
$$

или в форме невязки:

$$
F(t_c) = q_{\text{эл}} - q_{\text{конв}} - q_{\text{рад}} = 0
$$

---

## 2. Тепловые потоки

### 2.1. Электрический тепловой поток (закон Джоуля–Ленца с ТКС)

$$
q_{\text{эл}} = \frac{I^2 \cdot R_{20}}{b} \cdot \left[1 + (t_c - 20) \cdot \alpha_R \right]
$$

где:
- $I$ — сила тока, А
- $R_{20}$ — погонное электрическое сопротивление при 20 °C, Ом/м
- $b$ — ширина пластины, м
- $\alpha_R$ — температурный коэффициент сопротивления (ТКС), К⁻¹
- $t_c$ — температура стенки, °C

### 2.2. Конвективный тепловой поток

$$
q_{\text{конв}} = \alpha_x \cdot (t_c - t_{\text{ж}})
$$

где $\alpha_x$ — местный коэффициент теплоотдачи, Вт/(м²·К).

### 2.3. Радиационный тепловой поток

$$
q_{\text{рад}} = C_{\text{пр}} \cdot \left[\left(\frac{T_c}{100}\right)^4 - \left(\frac{T_{\text{ж}}}{100}\right)^4\right]
$$

где:
- $C_{\text{пр}}$ — приведённый коэффициент излучения, Вт/(м²·К⁴) (по умолчанию 2.0)
- $T_c = t_c + 273.15$ — температура стенки, К
- $T_{\text{ж}} = t_{\text{ж}} + 273.15$ — температура среды, К

---

## 3. Коэффициент теплоотдачи

$$
\alpha_x = \frac{Nu_x \cdot \lambda}{x}
$$

где $\lambda$ — теплопроводность воздуха при определяющей температуре, $x$ — координата по высоте.

---

## 4. Критериальные числа

### 4.1. Коэффициент объёмного расширения (модель идеального газа)

$$
\beta = \frac{1}{t_{\text{опр}} + 273{,}15}
$$

### 4.2. Число Грасгофа

$$
Gr_x = \frac{g \cdot \beta \cdot (t_c - t_{\text{ж}}) \cdot x^3}{\nu^2}
$$

### 4.3. Число Рэлея

$$
Ra_x = Gr_x \cdot Pr
$$

### 4.4. Определяющая температура

Выбор определяющей температуры $t_{\text{опр}}$ зависит от метода расчёта:

| Метод | $t_{\text{опр}}$ | Обоснование |
|-------|-------------------|-------------|
| Методичка Керимова | $t_{\text{ж}}$ | Свойства при температуре среды + поправка $\varepsilon_t$ |
| Задачник Кузнецова | $\frac{t_c + t_{\text{ж}}}{2}$ | Плёночная температура |
| Черчилль–Чу | $\frac{t_c + t_{\text{ж}}}{2}$ | Плёночная температура |

Физические свойства воздуха ($\nu$, $\lambda$, $Pr$, $\mu$, $\rho$, $c_p$) вычисляются при $t_{\text{опр}}$ через библиотеку **CoolProp**.

---

## 5. Корреляции для числа Нуссельта

Реализовано **три варианта** расчёта $Nu_x$.

### 5.1. Методичка Керимова (местный Nu, свойства при $t_{\text{ж}}$)

Источник: Р.В. Керимов. Лабораторная работа № 9 и 9а. МЭИ, 1992.

**Поправка на переменность свойств:**

$$
\varepsilon_t = \left(\frac{Pr_{\text{ж}}}{Pr_c}\right)^{0{,}25}
$$

**Ламинарный режим** ($Gr \cdot Pr \leq \text{crit}_1$, по умолчанию $10^9$):

$$
Nu_x = 0{,}60 \cdot (Gr \cdot Pr)^{0{,}25} \cdot \varepsilon_t
$$

**Турбулентный режим** ($Gr \cdot Pr \geq \text{crit}_2$, по умолчанию $6 \cdot 10^{10}$):

$$
Nu_x = 0{,}15 \cdot (Gr \cdot Pr)^{1/3} \cdot \varepsilon_t
$$

**Переходный режим** ($\text{crit}_1 < Gr \cdot Pr < \text{crit}_2$):

Линейная интерполяция по $\lg(Gr \cdot Pr)$:

$$
Nu_x = Nu_{\text{лам}}(\text{crit}_1) + \frac{\lg(GrPr) - \lg(\text{crit}_1)}{\lg(\text{crit}_2) - \lg(\text{crit}_1)} \cdot \left[Nu_{\text{турб}}(\text{crit}_2) - Nu_{\text{лам}}(\text{crit}_1)\right]
$$

### 5.2. Задачник Кузнецова (местный Nu, $q_c = \text{const}$, свойства при $t_{\text{пл}}$)

Источник: Ф.Ф. Цветков, Р.В. Керимов, В.И. Величко. Задачник по тепломассообмену. МЭИ, 2008. Формулы (3.4), (3.6), (3.8).

**Функция $\Phi(Pr)$ для граничного условия $q_c = \text{const}$:**

$$
\Phi(Pr) = \left[1 + \left(\frac{0{,}437}{Pr}\right)^{9/16}\right]^{-16/9}
$$

Для воздуха ($Pr \approx 0{,}7$): $\Phi \approx 0{,}363$.

**Ламинарный режим** ($Ra \leq \text{crit}_1$, по умолчанию $10^9$):

$$
Nu_x = 0{,}563 \cdot \left[Ra_x \cdot \Phi(Pr)\right]^{0{,}25}
$$

**Турбулентный режим** ($Ra \geq \text{crit}_2$, по умолчанию $10^{12}$):

$$
Nu_x = 0{,}15 \cdot \left[Ra_x \cdot \Phi(Pr)\right]^{1/3}
$$

**Переходный режим** ($\text{crit}_1 < Ra < \text{crit}_2$):

Линейная интерполяция по $\lg(Ra)$ (аналогично п. 5.1).

### 5.3. Черчилль–Чу (средний Nu, весь диапазон Ra)

Источник: Формула (3.9) задачника. Обобщённая корреляция для $t_c = \text{const}$.

**Функция $\Psi(Pr)$:**

$$
\Psi(Pr) = \left[1 + \left(\frac{0{,}492}{Pr}\right)^{9/16}\right]^{-16/9}
$$

**Средний Nu:**

$$
\overline{Nu} = \left[0{,}825 + \frac{0{,}387 \cdot Ra_l^{1/6}}{\left[1 + \left(\frac{0{,}492}{Pr}\right)^{9/16}\right]^{8/27}}\right]^2
$$

что эквивалентно:

$$
\overline{Nu} = \left[0{,}825 + 0{,}387 \cdot \left(Ra \cdot \Psi(Pr)\right)^{1/6}\right]^2
$$

> **Важно:** эта формула даёт **средний** $\overline{Nu}$ по всей длине пластины, а не местный $Nu_x$. При использовании в программе она применяется в каждой точке $x$ как оценка сверху.

---

## 6. Алгоритм решения

### 6.1. Метод поиска корня

Для каждой координаты $x_i$ решается нелинейное уравнение:

$$
F(t_c) = q_{\text{эл}}(t_c) - q_{\text{конв}}(t_c) - q_{\text{рад}}(t_c) = 0
$$

Используется метод **brentq** (метод Брента) из библиотеки `scipy.optimize` — комбинация метода бисекции и обратной квадратичной интерполяции. Гарантирует сходимость при условии, что $F$ меняет знак на интервале.

### 6.2. Интервал поиска

Начальный интервал: $[t_{\text{ж}} + 1, \; t_{\text{ж}} + 500]$ °C.

Если $F$ не меняет знак на этом интервале — автоматическое расширение до $t_{\text{ж}} + 1000$ °C.

### 6.3. Точность

$$
|t_c^{(k+1)} - t_c^{(k)}| < 10^{-8} \text{ °C}
$$

### 6.4. Свойства функции $F(t_c)$

Функция $F(t_c)$ **монотонно убывает** при стандартных параметрах:
- При $t_c \to t_{\text{ж}}$: $q_{\text{эл}} > 0$, $q_{\text{конв}} \to 0$, $q_{\text{рад}} \to 0$ → $F > 0$
- При $t_c \to \infty$: $q_{\text{конв}}$ и $q_{\text{рад}}$ растут быстрее $q_{\text{эл}}$ → $F < 0$

Корень **единственный** для каждого $x$.

### 6.5. Порядок расчёта в одной точке $x$

```
1. Задать начальное приближение: t_lo, t_hi
2. Вычислить F(t_lo) и F(t_hi)
3. Проверить смену знака: F(t_lo) · F(t_hi) < 0
4. Найти корень t_c методом Брента
5. Пересчитать все промежуточные величины:
   a. t_опр → свойства воздуха (ν, λ, Pr) через CoolProp
   b. β → Gr → Ra → GrPr
   c. ε_t (или Φ, Ψ) → Nu → α
   d. q_эл, q_конв, q_рад → невязка
6. Классифицировать режим: лам / перех / турб
```

### 6.6. Расчёт по всей пластине

Равномерная сетка из $N$ точек от $x_{\min}$ до $L$:

$$
x_i = x_{\min} + \frac{i}{N-1} \cdot (L - x_{\min}), \quad i = 0, 1, \ldots, N-1
$$

---

## 7. Анализ турбулентного режима

### 7.1. Прямая задача: найти $x_{\text{крит}}$

Найти координату $x$, при которой $Gr \cdot Pr = \text{crit}_2$ (граница турбулентного режима).

**Алгоритм:**
1. Для заданного $x$ решить тепловой баланс → получить $t_c(x)$
2. Вычислить $GrPr(x)$ при найденном $t_c$
3. Решить уравнение $GrPr(x) - \text{crit}_2 = 0$ методом Брента по $x$

### 7.2. Обратная задача: найти минимальный ток $I_{\text{крит}}$

Найти минимальный ток $I$, при котором турбулентный режим достигается на пластине высотой $L$.

**Алгоритм:**
1. Сканирование: вычислить $x_{\text{крит}}(I)$ для 40 значений $I$ в диапазоне $[I_{\min}, I_{\max}]$
2. Найти $I$, при котором $x_{\text{крит}} \leq L$
3. Уточнить методом Брента на найденном интервале

> Функция $x_{\text{крит}}(I)$ **не монотонна** — при больших $I$ свойства воздуха ухудшают $Gr$, поэтому сначала выполняется сканирование.

---

## 8. Параметры по умолчанию (стенд Керимова)

| Параметр | Обозначение | Значение | Единица |
|----------|-------------|----------|---------|
| Сила тока | $I$ | 300 | А |
| Погонное сопротивление | $R_{20}$ | 3.34·10⁻³ | Ом/м |
| ТКС | $\alpha_R$ | 1.088·10⁻³ | К⁻¹ |
| Ширина пластины | $b$ | 205 | мм |
| Высота пластины | $L$ | 1540 | мм |
| Температура среды | $t_{\text{ж}}$ | 20 | °C |
| Ускорение свободного падения | $g$ | 9.80665 | м/с² |
| Приведённый коэфф. излучения | $C_{\text{пр}}$ | 2.0 | Вт/(м²·К⁴) |
| Давление | $P$ | 101325 | Па |
| Начальная координата | $x_{\min}$ | 10 | мм |
| Количество точек | $N$ | 100 | — |

### Границы режимов по умолчанию

| Корреляция | $\text{crit}_1$ (лам→перех) | $\text{crit}_2$ (перех→турб) |
|---|---|---|
| Методичка Керимова | $10^9$ | $6 \cdot 10^{10}$ |
| Задачник Кузнецова | $10^9$ | $10^{12}$ |

---

## 9. Физические свойства воздуха

Свойства сухого воздуха ($\nu$, $\lambda$, $Pr$, $\mu$, $\rho$, $c_p$) вычисляются через библиотеку **CoolProp** при заданных температуре $T$ [К] и давлении $P$ [Па].

Используемая среда: `'Air'` (сухой воздух).

---

## 10. Структура программы

| Файл | Назначение |
|------|------------|
| `app.py` | Streamlit-интерфейс, ввод параметров, визуализация |
| `solver.py` | Решатель теплового баланса (brentq), расчёт по всей пластине |
| `correlations.py` | Критериальные уравнения (Nu, Gr, Ra, тепловые потоки) |
| `properties.py` | Обёртка CoolProp для свойств воздуха |
| `config.py` | Параметры по умолчанию, диапазоны, константы |
| `plotting.py` | Графики Plotly (профили t_c, α, зоны режимов) |
| `calculation_log.py` | Подробный вывод хода расчёта (LaTeX-формулы) |
| `turbulence_solver.py` | Анализ перехода к турбулентности |
| `qconst_page.py` | UI вкладки «Стенд q_w = const» (силиконовые нагреватели) |

---

## 11. Режим заданной плотности теплового потока ($q_w = \text{const}$)

### 11.1. Постановка задачи

В рамках ВКР (НИУ «МЭИ», раздел 2.2) проектируется новый стенд, отличающийся от стенда Керимова схемой подвода тепла:

- 4 силиконовых нагревателя на тыльной стороне теплораспределителя из АМг3 (толщина 12 мм);
- суммарная электрическая мощность фиксирована: $N_{\text{эл}} = 1{,}0$ кВт (по 250 Вт на каждый нагреватель);
- лицевая поверхность пластины покрыта высокотемпературной кремнийорганической эмалью CERTA, $\varepsilon = 0{,}95$, рабочая температура до 600 °C;
- геометрия: $H = 2000$ мм, $b = 100$ мм, $A = H \cdot b = 0{,}2$ м².

Поскольку питание силиконовых нагревателей **не имеет температурного коэффициента сопротивления** в рабочей цепи (нагреватель работает в режиме источника постоянной мощности через регулятор), на лицевой поверхности обеспечивается граничное условие 2-го рода:

$$q_w = \frac{N_{\text{эл}}}{A} = \frac{N_{\text{эл}}}{b \cdot L} = \text{const}.$$

При $N_{\text{эл}} = 1$ кВт и $A = 0{,}2$ м² номинальное $q_w = 5000$ Вт/м².

### 11.2. Уравнение теплового баланса

В отличие от стенда Керимова (раздел 2), где $q_{\text{эл}}(t_c)$ зависит от температуры стенки через ТКС, здесь $q_w$ не зависит от $t_c$. Уравнение баланса в каждом сечении $x$ принимает вид:

$$F(t_c) = q_w - q_{\text{конв}}(t_c) - q_{\text{рад}}(t_c) = 0, \qquad q_w = \text{const}.$$

Конвективная и радиационная составляющие — те же, что в разделах 2.2 и 2.3.

### 11.3. Связь степени черноты и приведённого коэффициента излучения

Приведённый коэффициент излучения для серой поверхности в большом объёме связан со степенью черноты соотношением:

$$C_{\text{пр}} = \varepsilon \cdot \sigma_0 = \varepsilon \cdot 5{,}67 \;\; \text{Вт/(м}^2 \cdot \text{К}^4\text{)},$$

где $\sigma_0 = 5{,}67$ — постоянная Стефана–Больцмана в форме «деление на $100^4$» (как используется в `calc_q_rad`). Для эмали CERTA: $C_{\text{пр}} = 0{,}95 \cdot 5{,}67 \approx 5{,}39$ Вт/(м²·К⁴).

### 11.4. Алгоритм решения

Используется тот же метод Брента, что и для стенда Керимова, но:

- невязка $F(t_c)$ упрощённая (см. 11.2), поскольку первый член не зависит от $t_c$;
- интервал поиска корня расширен до $[t_{\text{ж}} + 1, t_{\text{ж}} + 1500]$ с фолбэком до $t_{\text{ж}} + 2000$ °C, поскольку при $q_w \sim 5000$ Вт/м² у нижней кромки температура стенки может достигать 350–400 °C;
- расчёт реализован отдельной функцией `solve_plate_qconst` в `solver.py`, не нарушая существующий `solve_plate`.

Все 8 корреляций для числа Нуссельта (раздел 5) используются без изменений: они работают через критерии $Ra$, $GrPr$ независимо от граничного условия.

### 11.5. Двустендовая верификация

Чтобы убедиться, что новый решатель согласован с существующим, в UI предусмотрена сводка: при выборе тока $I$ для стенда Керимова автоматически вычисляется среднее значение $\bar{q}_{\text{эл}} = \frac{1}{L} \int_0^L q_{\text{эл}}(t_c(x))\,dx$, которое подаётся как $q_w$ в новый решатель. В средней зоне $x \in [0{,}2L,\, 0{,}8L]$ (вдали от краевых эффектов) профили $t_c(x)$ обоих стендов должны совпадать с точностью до различий, обусловленных переменностью $q_{\text{эл}}$ из-за ТКС (~5 %).

### 11.6. Параметры по умолчанию для нового стенда

| Параметр | Значение | Источник |
|----------|----------|----------|
| $N_{\text{эл}}$ | 1000 Вт | проект ВКР, 4 × 250 Вт |
| $b$ | 100 мм | геометрия |
| $L$ | 2000 мм | геометрия |
| $\varepsilon$ | 0.95 | паспорт эмали CERTA |
| $C_{\text{пр}}$ | 5.39 Вт/(м²·К⁴) | $\varepsilon \cdot 5.67$ |
| $q_w$ | 5000 Вт/м² | $N_{\text{эл}} / (b \cdot L)$ |
| $x_{\min}$ | 5 мм | устранение сингулярности при $x \to 0$ |

Серийное исследование выполняется при долях номинальной мощности 25 / 50 / 75 / 100 % для оценки чувствительности профиля $t_c(x)$ к нагрузке.

### 11.7. Ожидаемые результаты (валидация)

При $q_w = 5000$ Вт/м² (максимум, корреляция Кузнецова, $\varepsilon = 0{,}95$, $t_{\text{ж}} = 20$ °C):

- переход $Ra_x = 10^9$ происходит в нижней трети пластины;
- $Ra_{\max} \approx 4 \cdot 10^{10}$ у верхней кромки;
- $t_{w,\max} \approx 250\text{–}260$ °C;
- развитый переходный/турбулентный режим на верхней части пластины.

Эти значения служат критерием согласованности расчёта с предварительным анализом, выполненным в ВКР, и обоснованием выбора $H = 2000$ мм.
